apiVersion: v1
kind: Service
metadata:
  name: {{ include "avertem.fullname" . }}
  labels:
    {{- include "avertem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "avertem.selectorLabels" . | nindent 4 }}
